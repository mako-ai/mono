import { Box, Typography } from "@mui/material";

function Collections() {
  return (
    <Box
      sx={{
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "text.secondary",
      }}
    >
      <Typography>Collections will go here</Typography>
    </Box>
  );
}

export default Collections;
