version: "3.8"

services:
  revops:
    image: revops
    container_name: revops
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config
      - ./consoles:/app/consoles
  # mongodb:
  #   image: mongo:7.0
  #   container_name: mongo
  #   restart: always
  #   ports:
  #     - "27018:27017"
  #   volumes:
  #     - mongodb_data:/data/db
  #   networks:
  #     - close-network

  # mongo-express:
  #   image: mongo-express
  #   container_name: mongo-express
  #   restart: always
  #   ports:
  #     - "8081:8081"
  #   environment:
  #     ME_CONFIG_MONGODB_URL: mongodb://mongodb:27017/
  #   depends_on:
  #     - mongodb
  #   networks:
  #     - close-network

  # Web Backend (API) - Commented out for local development
  # web-backend:
  #   build:
  #     context: ./api
  #     dockerfile: Dockerfile.dev
  #   container_name: analytics-platform-api-dev
  #   volumes:
  #     - ./api:/app
  #     - /app/node_modules
  #   ports:
  #     - "3001:3001"
  #   environment:
  #     - NODE_ENV=development
  #     - MONGODB_DATABASE=multi_tenant_analytics
  #     - MONGODB_CONNECTION_STRING=mongodb://mongodb:27017
  #     - PORT=3001
  #   depends_on:
  #     - mongodb
  #   networks:
  #     - close-network
  #   command: npm run dev

  # Next.js Web App - Commented out for local development
  # web-app:
  #   build:
  #     context: ./web
  #     dockerfile: Dockerfile.dev
  #   container_name: analytics-platform-web-app
  #   volumes:
  #     - ./web:/app
  #     - /app/node_modules
  #     - /app/.next
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=development
  #     - NEXT_PUBLIC_API_URL=http://localhost:3001
  #     - MONGODB_DATABASE=multi_tenant_analytics
  #     - MONGODB_CONNECTION_STRING=mongodb://mongodb:27017
  #   depends_on:
  #     - mongodb
  #     - web-backend
  #   networks:
  #     - close-network
  #   command: npm run dev

  # Additional development services can be added here
  # redis:
  #   image: redis:alpine
  #   container_name: analytics-platform-redis
  #   ports:
  #     - "6379:6379"
  #   networks:
  #     - close-network

volumes:
  mongodb_data:

networks:
  close-network:
    driver: bridge
